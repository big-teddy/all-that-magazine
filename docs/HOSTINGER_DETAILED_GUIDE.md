# Hostinger 상세 설정 가이드

## 📋 목차
1. [Step 1: hPanel 로그인](#step-1-hpanel-로그인)
2. [Step 2: DNS Zone Editor 설정](#step-2-dns-zone-editor-설정)
3. [Step 3: Subdomain 생성](#step-3-subdomain-생성)
4. [Step 4: WordPress URL 변경](#step-4-wordpress-url-변경)
5. [Step 5: 설정 확인](#step-5-설정-확인)

---

## Step 1: hPanel 로그인

### 1-1. Hostinger 로그인
1. 브라우저에서 https://hpanel.hostinger.com 접속
2. 이메일과 비밀번호로 로그인
3. 2단계 인증이 있다면 코드 입력

### 1-2. 대시보드 확인
로그인하면 다음과 같은 화면이 보입니다:
```
┌─────────────────────────────────────┐
│  Hostinger hPanel                   │
├─────────────────────────────────────┤
│  좌측 메뉴:                          │
│  - Home                             │
│  - Websites                         │
│  - Email                            │
│  - Domains                          │
│  - Files                            │
│  - Advanced                         │
│  - Help                             │
└─────────────────────────────────────┘
```

---

## Step 2: DNS Zone Editor 설정

### 2-1. Websites 메뉴로 이동
1. **좌측 메뉴**에서 **"Websites"** 클릭
2. 웹사이트 목록에서 **"allthatmagazine.com"** 찾기
3. 해당 도메인 클릭 또는 **"Manage"** 버튼 클릭

### 2-2. DNS Zone Editor 접근
1. 좌측 메뉴에서 **"Advanced"** 섹션 찾기
2. **"DNS / Name Servers"** 클릭
3. 또는 상단 탭에서 **"Advanced"** → **"DNS"** 클릭

화면 예시:
```
┌──────────────────────────────────────────┐
│  allthatmagazine.com                     │
├──────────────────────────────────────────┤
│  탭: Overview | Files | Advanced | ...   │
│                                          │
│  Advanced 메뉴:                          │
│  ├─ DNS / Name Servers ⟵ 여기 클릭      │
│  ├─ SSL                                  │
│  ├─ Redirects                            │
│  ├─ Subdomains                           │
│  └─ ...                                  │
└──────────────────────────────────────────┘
```

### 2-3. DNS Zone Editor 화면
DNS Zone Editor에 들어가면 현재 설정된 DNS 레코드들이 표시됩니다:

```
┌──────────────────────────────────────────────────────┐
│  DNS Zone Editor                                     │
├──────────────────────────────────────────────────────┤
│  [+ Add Record] 버튼                                 │
│                                                      │
│  현재 레코드:                                        │
│  ┌────────────────────────────────────────────────┐ │
│  │ Type │ Name │ Points to      │ TTL    │ Actions││
│  ├──────┼──────┼────────────────┼────────┼────────┤│
│  │ A    │ @    │ 45.130.228.131 │ 14400  │ [수정] ││
│  │ A    │ www  │ 45.130.228.131 │ 14400  │ [수정] ││
│  │ MX   │ @    │ mx.hostinger...│ 14400  │ [수정] ││
│  └──────┴──────┴────────────────┴────────┴────────┘ │
└──────────────────────────────────────────────────────┘
```

### 2-4. wp 서브도메인 A 레코드 추가

**중요:** 아직 기존 레코드는 건드리지 마세요!

1. **"+ Add Record"** 또는 **"Add New Record"** 버튼 클릭

2. 팝업 창이 뜨면 다음과 같이 입력:

```
┌─────────────────────────────────┐
│  Add DNS Record                 │
├─────────────────────────────────┤
│  Type: [A Record ▼]             │
│                                 │
│  Name: [wp________________]     │
│                                 │
│  Points to:                     │
│  [45.130.228.131__________]     │
│                                 │
│  TTL (optional):                │
│  [14400___________________]     │
│                                 │
│  [ Cancel ]  [Add Record]       │
└─────────────────────────────────┘
```

**입력값:**
- **Type**: `A Record` 선택 (드롭다운)
- **Name**: `wp` (소문자, @ 없이)
- **Points to**: `45.130.228.131`
- **TTL**: `14400` (기본값, 4시간)

3. **"Add Record"** 버튼 클릭

4. 성공 메시지 확인:
```
✓ DNS record has been added successfully
```

5. 추가된 레코드 확인:
```
┌────────────────────────────────────────────────┐
│ Type │ Name │ Points to      │ TTL    │ Actions│
├──────┼──────┼────────────────┼────────┼────────│
│ A    │ @    │ 45.130.228.131 │ 14400  │ [수정] │
│ A    │ www  │ 45.130.228.131 │ 14400  │ [수정] │
│ A    │ wp   │ 45.130.228.131 │ 14400  │ [수정] │ ⟵ 새로 추가됨
└──────┴──────┴────────────────┴────────┴────────┘
```

⏰ **DNS 전파 시간:** 보통 10분~1시간, 최대 24-48시간

---

## Step 3: Subdomain 생성

### 3-1. Subdomains 메뉴로 이동

1. **좌측 메뉴** 또는 **상단 탭**에서 **"Subdomains"** 찾기
   - 경로 1: **Advanced** → **Subdomains**
   - 경로 2: **Domains** → **Subdomains**

### 3-2. Subdomain 생성 화면

```
┌──────────────────────────────────────────────┐
│  Subdomains                                  │
├──────────────────────────────────────────────┤
│  [+ Create Subdomain] 버튼                   │
│                                              │
│  현재 서브도메인: (없음 또는 기존 목록)      │
└──────────────────────────────────────────────┘
```

### 3-3. 서브도메인 생성

1. **"+ Create Subdomain"** 또는 **"Add Subdomain"** 버튼 클릭

2. 폼 작성:

```
┌─────────────────────────────────────────────┐
│  Create Subdomain                           │
├─────────────────────────────────────────────┤
│  Subdomain:                                 │
│  [wp_____] . [allthatmagazine.com ▼]        │
│                                             │
│  Document Root (자동 생성됨):               │
│  /domains/allthatmagazine.com/public_html/wp│
│  또는                                       │
│  /public_html/wp                            │
│                                             │
│  ⚠️ 중요: Document Root 수정하기            │
│  기존 WordPress와 같은 경로 사용:           │
│  /domains/allthatmagazine.com/public_html   │
│                                             │
│  [ Cancel ]  [ Create ]                     │
└─────────────────────────────────────────────┘
```

**입력값:**
- **Subdomain**: `wp`
- **Domain**: `allthatmagazine.com` (드롭다운에서 선택)
- **Document Root**: `/domains/allthatmagazine.com/public_html`

  ⚠️ **매우 중요!**
  - 자동으로 `/public_html/wp`로 설정될 수 있는데
  - 반드시 `/public_html`만 남기고 `/wp` 제거
  - 기존 WordPress와 같은 디렉토리를 사용해야 함

3. **"Create"** 버튼 클릭

4. 생성 확인:
```
✓ Subdomain wp.allthatmagazine.com has been created
```

### 3-4. 서브도메인 목록 확인

```
┌──────────────────────────────────────────────────────┐
│  Subdomains                                          │
├──────────────────────────────────────────────────────┤
│  wp.allthatmagazine.com                              │
│  Document Root: /public_html                         │
│  [Manage] [Delete]                                   │
└──────────────────────────────────────────────────────┘
```

---

## Step 4: WordPress URL 변경

### 4-1. WordPress 관리자 로그인

⚠️ **중요:** 아직 `wp.allthatmagazine.com`이 작동하지 않을 수 있습니다.
DNS 전파가 완료될 때까지 기존 URL 사용:

1. https://allthatmagazine.com/wp-admin 접속
2. 관리자 계정으로 로그인

### 4-2. General Settings로 이동

```
WordPress 관리자 대시보드
┌──────────────────────────────────────┐
│  좌측 메뉴:                          │
│  - Dashboard                         │
│  - Posts                             │
│  - Media                             │
│  - Pages                             │
│  - Comments                          │
│  - Appearance                        │
│  - Plugins                           │
│  - Users                             │
│  - Tools                             │
│  - Settings ⟵ 여기 클릭              │
│    └─ General ⟵ 여기 클릭            │
└──────────────────────────────────────┘
```

### 4-3. URL 변경

General Settings 페이지에서:

```
┌──────────────────────────────────────────────┐
│  General Settings                            │
├──────────────────────────────────────────────┤
│  Site Title:                                 │
│  [All That Magazine________________]         │
│                                              │
│  Tagline:                                    │
│  [Premium Wellness Lifestyle Tech___]        │
│                                              │
│  WordPress Address (URL): ⟵ 이거 변경       │
│  [https://allthatmagazine.com_______]        │
│  ↓                                           │
│  [https://wp.allthatmagazine.com____]        │
│                                              │
│  Site Address (URL): ⟵ 이거도 변경          │
│  [https://allthatmagazine.com_______]        │
│  ↓                                           │
│  [https://wp.allthatmagazine.com____]        │
│                                              │
│  Administration Email Address:               │
│  [your-email@example.com____________]        │
│                                              │
│  (스크롤 다운)                               │
│  [Save Changes] ⟵ 맨 아래 저장 버튼         │
└──────────────────────────────────────────────┘
```

**변경할 내용:**
- **WordPress Address (URL)**:
  - Before: `https://allthatmagazine.com`
  - After: `https://wp.allthatmagazine.com`

- **Site Address (URL)**:
  - Before: `https://allthatmagazine.com`
  - After: `https://wp.allthatmagazine.com`

### 4-4. 저장 및 재로그인

1. 페이지 맨 아래로 스크롤
2. **"Save Changes"** 버튼 클릭
3. ⚠️ **자동으로 로그아웃됩니다!**

### 4-5. 새 URL로 재로그인

DNS 전파가 완료되면:
1. https://wp.allthatmagazine.com/wp-admin 접속
2. 다시 로그인

⏰ DNS 전파 대기 중이면:
- 10분~1시간 후 시도
- `https://dnschecker.org`에서 `wp.allthatmagazine.com` 확인

---

## Step 5: 설정 확인

### 5-1. DNS 전파 확인

**터미널에서 확인:**
```bash
# wp 서브도메인 확인
dig +short wp.allthatmagazine.com

# 예상 결과: 45.130.228.131
```

**온라인 도구 사용:**
1. https://dnschecker.org 접속
2. `wp.allthatmagazine.com` 입력
3. 전 세계 DNS 서버에서 전파 상태 확인

```
┌────────────────────────────────────────┐
│  DNS Checker                           │
├────────────────────────────────────────┤
│  Hostname: wp.allthatmagazine.com      │
│  Type: A                               │
│                                        │
│  Results:                              │
│  ✓ USA (New York):   45.130.228.131   │
│  ✓ Europe (London):  45.130.228.131   │
│  ✓ Asia (Tokyo):     45.130.228.131   │
│  ✓ Australia:        45.130.228.131   │
└────────────────────────────────────────┘
```

### 5-2. WordPress 접속 테스트

**1. 관리자 페이지:**
```
https://wp.allthatmagazine.com/wp-admin
```
- ✅ 로그인 성공
- ✅ 대시보드 정상 표시

**2. GraphQL 엔드포인트:**
```
https://wp.allthatmagazine.com/graphql
```
- ✅ GraphiQL IDE 표시됨

**3. REST API:**
```
https://wp.allthatmagazine.com/wp-json
```
- ✅ JSON 응답 반환

### 5-3. 브라우저 테스트

브라우저에서 다음 URL들을 확인:

```
✓ https://wp.allthatmagazine.com
  → WordPress 사이트 표시 (현재는 이것도 표시됨)

✓ https://wp.allthatmagazine.com/wp-admin
  → 로그인 페이지 또는 대시보드

✓ https://wp.allthatmagazine.com/graphql
  → GraphiQL 인터페이스
```

---

## 🚨 Troubleshooting

### 문제 1: wp.allthatmagazine.com이 열리지 않음

**원인:** DNS 전파 미완료

**해결책:**
1. 10분~1시간 대기
2. DNS 전파 확인: https://dnschecker.org
3. 브라우저 캐시 삭제:
   - Chrome: `Cmd+Shift+Delete` (Mac) / `Ctrl+Shift+Delete` (Windows)
   - "캐시된 이미지 및 파일" 선택 후 삭제
4. DNS 캐시 플러시:
   ```bash
   # Mac
   sudo dscacheutil -flushcache
   sudo killall -HUP mDNSResponder

   # Windows
   ipconfig /flushdns
   ```

### 문제 2: WordPress URL 변경 후 로그인 불가

**원인:** DNS 전파 전에 URL 변경

**해결책 A: wp-config.php 수정 (임시)**

1. Hostinger File Manager 접속:
   - hPanel → Files → File Manager

2. `wp-config.php` 파일 찾기:
   - `/public_html/wp-config.php`

3. 편집 모드로 열기 (우클릭 → Edit)

4. 파일 맨 위에 다음 코드 추가:
   ```php
   <?php
   // 임시: URL 강제 설정
   define('WP_HOME', 'https://allthatmagazine.com');
   define('WP_SITEURL', 'https://allthatmagazine.com');
   ```

5. 저장 후 https://allthatmagazine.com/wp-admin 로그인

6. Settings → General에서 URL 다시 변경

7. 위 코드 삭제

**해결책 B: 데이터베이스 직접 수정**

1. hPanel → Advanced → phpMyAdmin

2. 데이터베이스 선택 (보통 `u123456789_wp` 형식)

3. `wp_options` 테이블 선택

4. `siteurl`과 `home` 행 찾기

5. 값을 `https://wp.allthatmagazine.com`으로 수정

### 문제 3: Subdomain Document Root 잘못 설정

**증상:** wp.allthatmagazine.com 접속 시 빈 페이지 또는 404

**해결책:**

1. hPanel → Subdomains

2. `wp.allthatmagazine.com` 찾기

3. **Manage** 또는 **Edit** 클릭

4. Document Root 확인:
   - 잘못: `/public_html/wp`
   - 올바름: `/public_html`

5. 수정 후 저장

### 문제 4: SSL 인증서 오류

**증상:** "Your connection is not private" 경고

**해결책:**

1. hPanel → SSL

2. `wp.allthatmagazine.com` 서브도메인 확인

3. SSL 인증서 자동 발급 대기 (보통 5-10분)

4. 또는 수동으로 발급:
   - **SSL/TLS** → **Manage SSL**
   - `wp.allthatmagazine.com` 선택
   - **Install SSL** 클릭

---

## ✅ 완료 체크리스트

Hostinger 설정이 모두 완료되었는지 확인:

- [ ] DNS Zone Editor에 `wp` A 레코드 추가 완료
- [ ] `wp` 서브도메인 생성 완료
- [ ] Document Root가 `/public_html`로 설정됨
- [ ] WordPress Settings에서 URL 변경 완료
- [ ] `dig +short wp.allthatmagazine.com` 결과: `45.130.228.131`
- [ ] https://wp.allthatmagazine.com 접속 성공
- [ ] https://wp.allthatmagazine.com/wp-admin 로그인 성공
- [ ] https://wp.allthatmagazine.com/graphql 접속 성공

모두 체크되면 Hostinger 설정 완료! 🎉

다음 단계: **Vercel 도메인 연결**로 진행하세요.

---

**마지막 업데이트:** 2025-11-03
**작성자:** Claude Code
